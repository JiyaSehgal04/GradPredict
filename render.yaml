services:
  - type: web
    name: gradpredict-api
    runtime: python
    rootDir: backend
    buildCommand: pip install -r requirements.txt
    startCommand: python3 api.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
